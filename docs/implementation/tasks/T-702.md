# Task T-702 — `app/infrastructure/redis/` (+ Cache adapter)

> Section: **S07 — Infrastructure base**
>
> Before you start, read [`../rules.md`](../rules.md) (global rules, stop signals, allowed-files discipline, command discipline, lower-complexity model instructions).
> After you finish, self-check against [`../review.md`](../review.md) (final review checklist, architecture compliance).

---


Purpose: async Redis client + `Cache` adapter implementing `app.platform.cache.ports.Cache`.

Depends on: T-602, T-301.

Allowed files: `app/infrastructure/redis/{__init__,client,cache}.py`, `app/infrastructure/redis/tests/test_redis_cache.py`.

> **Note (test basename)**: use `test_redis_cache.py` (not the generic
> `test_cache.py`). Test module basenames must be globally unique across
> the project (see review.md and patterns.md).

Forbidden: do not import `redis` outside this package.

Implementation requirements: `build_client(settings) -> Redis`; `RedisCache` implements every method of `app.platform.cache.ports.Cache`; serialization keeps payloads as bytes; TTL respected.

> **Structural conformance (ADR-0026 / patterns.md)**: `RedisCache` must
> satisfy the `Cache` Protocol **structurally**. Do **not** inherit from
> `Cache` (`class RedisCache(Cache):` is forbidden for this task). Contract
> tests may still use `isinstance(instance, Cache)` because `Cache` is
> `@runtime_checkable`.

> **`.importlinter` (ADR-0026)**: adapter-to-port imports
> (`app.infrastructure.redis.* -> app.platform.cache.ports`) are allowed by
> the project-level infrastructure adapter-ring contract. **This task must
> not edit `.importlinter`.** If `lint-imports` fails for the adapter-to-port
> direction, stop and report — do not add `ignore_imports`, do not add a
> dynamic import, do not add a re-export shim, do not modify any layer or
> forbidden contract.

Tests required (integration): contract test for `Cache` runs against real Redis (Testcontainers).

Acceptance: `lint-imports` clean (with **no** `.importlinter` diff owned by this task); only `core.wiring.cache` imports this.

Commands: standard four + `make test-int`.

Common failure modes: leaking `Redis` object outside; exposing a `close`/`aclose` coroutine that is never called.

Note: This task **does not** wire the client into the application lifespan. `build_client(settings)` returns a Redis client with an awaitable `aclose()` shutdown hook, but installing that hook into `app/core/lifespan.py` and registering the Redis readiness probe are owned by T-708 (per rules.md §1 "Append-only edits to shared assembly files"). Do not touch `app/core/lifespan.py`, `app/core/container.py`, or `app/core/wiring/` from this task.

Review checklist: implements full port; exposes an async shutdown hook for T-708 to wire; does not itself edit lifespan or container.

---

### Cross-links

- Global rules and execution discipline: [`../rules.md`](../rules.md)
- Reviewer model instructions and final review checklist: [`../review.md`](../review.md)
- Plan overview, dependency map, and task index: [`../../../IMPLEMENTATION_PLAN.md`](../../../IMPLEMENTATION_PLAN.md)
