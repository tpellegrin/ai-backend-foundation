# Task T-1801 — Update `docs/architecture.md`, `docs/folder-structure.md`, `docs/dependency-graph.md`

> Section: **S18 — Documentation & hardening**
>
> Before you start, read [`../rules.md`](../rules.md) (global rules, stop signals, allowed-files discipline, command discipline, lower-complexity model instructions).
> After you finish, self-check against [`../review.md`](../review.md) (final review checklist, architecture compliance).

---


Purpose: align legacy docs with what Phase 2 actually ships (per scope §7).

Allowed files (exhaustive — exactly these three):

- `docs/architecture.md`
- `docs/folder-structure.md`
- `docs/dependency-graph.md`

Forbidden:

- Do **not** modify any file under `docs/phase-2-revision/`. That pack is the frozen historical record of the Phase 2 revision decisions and must remain immutable. If a legacy doc contradicts the phase-2-revision pack, the legacy doc is wrong and must be updated to match; never the reverse.
- Do **not** modify any file under `docs/adr/`.
- Do **not** modify `AGENTS.md`, `docs/implementation/rules.md`, `docs/implementation/review.md`, or `IMPLEMENTATION_PLAN.md`.

Implementation requirements:

- Update tables and diagrams in the three legacy docs to reflect what Phase 2 actually ships: `platform/` (ports-only), `ai_governance` (domain + ports + service + persistence + API), Arq queue in Phase 2, the local-FS `BlobStorage` adapter (no S3 in Phase 2), and the current adapter list (OpenAI chat + embedding, pgvector vector store, Redis cache + rate limiter + idempotency store, Arq task queue, local-FS blob storage).
- Mark any section that referred to Phase 3 features (S3, Anthropic/other providers, `pydantic_ai`, multi-model router, agent framework, event bus) as **"Phase 3"**.
- Where a legacy doc's dependency diagram or module list conflicts with `docs/phase-2-revision/03-revised-dependency-graph.md`, rewrite the legacy doc to match that revision doc verbatim.

Acceptance: legacy docs no longer contradict the Phase 2 revision pack, and `docs/phase-2-revision/*` is byte-identical to its pre-task state.

---

### Cross-links

- Global rules and execution discipline: [`../rules.md`](../rules.md)
- Reviewer model instructions and final review checklist: [`../review.md`](../review.md)
- Plan overview, dependency map, and task index: [`../../../IMPLEMENTATION_PLAN.md`](../../../IMPLEMENTATION_PLAN.md)
