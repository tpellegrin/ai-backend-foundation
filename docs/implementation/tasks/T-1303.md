# Task T-1303 — `app/documents/chunkers/` (recursive token-aware)

> Section: **S13 — Documents (domain + persistence + API skeleton)**
>
> Before you start, read [`../rules.md`](../rules.md) (global rules, stop signals, allowed-files discipline, command discipline, lower-complexity model instructions).
> After you finish, self-check against [`../review.md`](../review.md) (final review checklist, architecture compliance).

---


Purpose: produce `Chunk` items with token-aware splitting.

Depends on: T-1301. `pyproject.toml` adds `tiktoken`.

Allowed files: `app/documents/chunkers/{__init__,recursive}.py`, `app/documents/chunkers/tests/*.py`, `pyproject.toml`, `uv.lock`.

Forbidden: `tiktoken` outside this folder.

Implementation requirements: recursive splitter using `tiktoken` encoder selected by configured embedding model family; overlap honored; deterministic chunk hashes. Edits to `pyproject.toml` / `uv.lock` are restricted to adding `tiktoken`.

Tests required: chunk count and overlap invariants on a known text; deterministic hash.

Otherwise standard.

---

### Cross-links

- Global rules and execution discipline: [`../rules.md`](../rules.md)
- Reviewer model instructions and final review checklist: [`../review.md`](../review.md)
- Plan overview, dependency map, and task index: [`../../../IMPLEMENTATION_PLAN.md`](../../../IMPLEMENTATION_PLAN.md)
