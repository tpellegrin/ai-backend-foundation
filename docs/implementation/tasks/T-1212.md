# Task T-1212 — `core/wiring/{llm,embeddings,governance}.py`

> Section: **S12 — ai_governance persistence/service/api + OpenAI adapters**
>
> Before you start, read [`../rules.md`](../rules.md) (global rules, stop signals, allowed-files discipline, command discipline, lower-complexity model instructions).
> After you finish, self-check against [`../review.md`](../review.md) (final review checklist, architecture compliance).

---


Purpose: bind ports to adapters and build `LlmService` from `Container`.

Depends on: T-1210, T-1211, T-1203, T-708.

Allowed files: `app/core/wiring/{llm,embeddings,governance}.py`, `app/core/container.py` (append-only), `app/core/lifespan.py` (append-only), `app/core/wiring/tests/test_wiring_llm.py`.

Forbidden: any of these may import `app.infrastructure.*`; no other module may. Edits to `container.py` and `lifespan.py` are strictly append-only per rules.md §1 "Append-only edits to shared assembly files" — no reordering or rewriting of fields/hooks landed by earlier tasks.

Implementation requirements:

- Build `LlmService` with: `ChatModel` adapter, `DefaultModelRouter` from settings, governance gate from `ai_governance.service`, prompt registry, system clock, cost calculator.
- Append to `app/core/container.py`: `llm: LlmService`, `embeddings: EmbeddingModel`, `governance: GovernanceService` fields (Protocol / service types).
- Append to `app/core/lifespan.py` any startup/shutdown hooks required by the OpenAI adapters (e.g. shared `httpx.AsyncClient` shutdown if a new one is introduced here) and — if applicable — a readiness probe for the LLM path (per rules.md §1 incremental-probe rule). Do not duplicate probes or clients already registered by earlier wiring tasks.

Tests required: wiring smoke test boots in-process and verifies the three new container fields are populated and typed by their Protocols / service classes.

Otherwise standard.

---

### Cross-links

- Global rules and execution discipline: [`../rules.md`](../rules.md)
- Reviewer model instructions and final review checklist: [`../review.md`](../review.md)
- Plan overview, dependency map, and task index: [`../../../IMPLEMENTATION_PLAN.md`](../../../IMPLEMENTATION_PLAN.md)
