# Task T-1701 — End-to-end golden-path test

> Section: **S17 — Golden-path integration**
>
> Before you start, read [`../rules.md`](../rules.md) (global rules, stop signals, allowed-files discipline, command discipline, lower-complexity model instructions).
> After you finish, self-check against [`../review.md`](../review.md) (final review checklist, architecture compliance).

---


Purpose: the single test that proves Phase 2 works.

Depends on: every previous task.

Allowed files: `tests/integration/test_golden_path.py`.

Implementation requirements

- Boot api+worker+postgres+redis via Testcontainers or `make up` (mark `slow`).
- Register a user, log in.
- `POST /api/v1/documents` uploading a 2-page PDF fixture; assert 202.
- Poll `GET /api/v1/documents/{id}` until status `ready` (≤ 30s).
- `POST /api/v1/rag/ask {"question": "<known content question>"}` using a **fake `ChatModel` and a fake `EmbeddingModel`** so CI never calls OpenAI.

**Fake-adapter override (per forward-audit patch P-14).** The wiring layer (T-1212 for LLM/embeddings) selects the fake adapters when `settings.openai.api_key` is the sentinel `SecretStr("fake")` **or** when a dedicated switch such as `APP_ENV=test` combined with `LLM_PROVIDER=fake` / `EMBEDDINGS_PROVIDER=fake` is set. The golden-path test must exercise **exactly one** of these mechanisms — do not invent a new one:

  1. **Preferred:** boot the app under Testcontainers with the fake-provider env vars set, so `app.core.wiring.llm.build_chat_model(settings)` and `app.core.wiring.embeddings.build_embedding_model(settings)` return the in-repo `FakeChatModel` / `FakeEmbeddingModel` (from T-1104 / T-1106) directly. No `app.dependency_overrides` needed.
  2. **Fallback (only if the wiring switch is not yet available at T-1701 landing time):** override `Container.llm` and `Container.embeddings` on the running app via `app.dependency_overrides` in the test fixture, binding them to `FakeChatModel()` / `FakeEmbeddingModel()`.

  Under no circumstances may the test call the real OpenAI API. `OPENAI_API_KEY` in the Testcontainers env must be the sentinel value, not a real credential. Assert that no outbound HTTP request leaves the process for `api.openai.com` (respx/httpx transport recording, or a sentinel check on the fake adapter's call counter).
- Assert: 200; `answer` non-empty; at least one citation; `citation.document_id` matches the uploaded id; `X-Request-ID` echoes if inbound.

Acceptance: Phase 2 acceptance criterion #3 holds.

Otherwise standard.

---

### Cross-links

- Global rules and execution discipline: [`../rules.md`](../rules.md)
- Reviewer model instructions and final review checklist: [`../review.md`](../review.md)
- Plan overview, dependency map, and task index: [`../../../IMPLEMENTATION_PLAN.md`](../../../IMPLEMENTATION_PLAN.md)
