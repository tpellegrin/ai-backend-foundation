# Task T-907A — Auth production refinement and bugfixes

Section: **S09 — Auth & Users**
Purpose: Fix rotation FK bug, integrate `Clock` for deterministic time, and clean up Auth API debt.

Depends on: T-907, T-203.

Allowed files:
- `app/auth/service.py`
- `app/auth/api.py`
- `app/auth/deps.py`
- `app/core/container.py`
- `app/core/lifespan.py`
- `app/core/wiring/clock.py`
- `app/auth/tests/test_service.py`
- `app/auth/tests/test_deps.py`
- `app/core/wiring/tests/test_wiring_auth.py`

Forbidden changes:
- Do not modify `app/auth/persistence.py`.
- Do not change database schemas or migrations.
- Do not implement new API endpoints.

Implementation requirements:
1. **Fix `AuthService.refresh` FK Violation**: Swap order of operations. Insert the new refresh token *before* updating the old token's `replaced_by` pointer.
2. **Clock Wiring**:
    - Create `app/core/wiring/clock.py` to wire `SystemClock`.
    - Add `clock: Clock | None` to `Container`.
    - Initialize `container.clock` in `lifespan.py`.
3. **Clock Integration**:
    - Update `AuthService` to accept `Clock` in `__init__`.
    - Replace all `datetime.now(UTC)` calls in `service.py` and `api.py` with `clock.now()`.
    - Update `get_auth_service` in `deps.py` to inject the clock from the container.
4. **API Cleanup**:
    - Remove T-908 TODOs from `app/auth/api.py`.
    - Ensure `expires_in` calculation uses the injected clock.

Tests required:
- Update `app/auth/tests/test_service.py` to pass with `Clock` (use `FixedClock` in mocks).
- Verify rotation order via unit test (mock `insert` must be called before `update`).
- Update `app/auth/tests/test_deps.py` to verify `Clock` injection in `get_auth_service` and `get_clock`.
- Update `app/core/wiring/tests/test_wiring_auth.py` to verify `Clock` is wired in `Container` during lifespan.

Acceptance:
- `make test` passes (unit).
- `AuthService.refresh` logic is corrected.
- `Clock` is correctly wired and injected.
