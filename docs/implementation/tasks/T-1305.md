# Task T-1305 — `app/documents/api.py` POST/GET

> Section: **S13 — Documents (domain + persistence + API skeleton)**
>
> Before you start, read [`../rules.md`](../rules.md) (global rules, stop signals, allowed-files discipline, command discipline, lower-complexity model instructions).
> After you finish, self-check against [`../review.md`](../review.md) (final review checklist, architecture compliance).

---


Purpose: ingestion entrypoint + status read.

Depends on: T-1304, T-906, T-501.

Allowed files: `app/documents/api.py`, `app/api/v1.py` (append-only), tests. In `app/api/v1.py`, **append** exactly the single line `router.include_router(documents.api.router, prefix="/documents", tags=["documents"])` to `build_v1_router()` (per rules.md §1 "Append-only edits to shared assembly files" and "The `/api/v1` router is incremental"). Do not modify any other line of `app/api/v1.py`. The module router itself is declared with no prefix; the `/documents` prefix is applied at the mount site. Effective route prefix is `/api/v1/documents`.

Forbidden: doing parse/embed work in the request path.

Implementation requirements

- `POST /api/v1/documents` (multipart upload **or** `{source_uri}` JSON): persist `Document(status="pending")`, store blob via `BlobStorage`, enqueue ingestion job via `TaskQueue` (job name `documents.ingest`), return `202 Accepted` with `{document_id, status: "pending", tracking_url}`.
- `GET /api/v1/documents/{id}` returns `{id, status, ready_at, failure_reason}`. 404 if not owned by current tenant.
- Idempotency: depends on `app.api.idempotency.IdempotencyKey`.

Tests required (api): 202 on upload; 401/422 paths; idempotent re-POST; 404 cross-tenant.

Otherwise standard.

---

### Cross-links

- Global rules and execution discipline: [`../rules.md`](../rules.md)
- Reviewer model instructions and final review checklist: [`../review.md`](../review.md)
- Plan overview, dependency map, and task index: [`../../../IMPLEMENTATION_PLAN.md`](../../../IMPLEMENTATION_PLAN.md)
