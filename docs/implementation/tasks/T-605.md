# Task T-605 — `app/platform/idempotency/ports.py` (`IdempotencyStore`)

> Section: **S06 — Platform ports**
>
> Before you start, read [`../rules.md`](../rules.md) (global rules, stop signals, allowed-files discipline, command discipline, lower-complexity model instructions).
> After you finish, self-check against [`../review.md`](../review.md) (final review checklist, architecture compliance).

---


Allowed files: `app/platform/idempotency/__init__.py`, `app/platform/idempotency/ports.py`, `app/platform/idempotency/tests/test_idempotency_ports.py`.

> Test file naming: use `test_idempotency_ports.py` (not the generic `test_ports.py`) to keep test-file basenames globally unique across `app/platform/*/tests/` and avoid pytest import-file mismatch errors. See [`../patterns.md`](../patterns.md).

Implementation requirements: `IdempotencyRecord` (`status: Literal["new","in_flight","done"], response_hash: str | None`). `IdempotencyStore.begin(key: str, ttl_s: int) -> IdempotencyRecord`; `complete(key, response_hash) -> None`; `get(key) -> IdempotencyRecord | None`.

Tests required: Protocol satisfiability test against an in-memory fake.

Otherwise (Purpose / Depends / Forbidden / Commands / Acceptance) analogous to T-601/602.


> All adapters live under `app/infrastructure/*`. They may import `app.platform.*` to implement the ports and `app.shared`/`app.observability`. They must not be imported anywhere except `app.core.wiring.*`.

---

### Cross-links

- Global rules and execution discipline: [`../rules.md`](../rules.md)
- Reviewer model instructions and final review checklist: [`../review.md`](../review.md)
- Plan overview, dependency map, and task index: [`../../../IMPLEMENTATION_PLAN.md`](../../../IMPLEMENTATION_PLAN.md)
