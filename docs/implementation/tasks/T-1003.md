# Task T-1003 — `app/prompts/api.py` (read-only)

> Section: **S10 — Prompts**
>
> Before you start, read [`../rules.md`](../rules.md) (global rules, stop signals, allowed-files discipline, command discipline, lower-complexity model instructions).
> After you finish, self-check against [`../review.md`](../review.md) (final review checklist, architecture compliance).

---


Purpose: admin read-only inspection.

Depends on: T-1001, T-906.

Allowed files: `app/prompts/api.py`, `app/api/v1.py` (append-only), tests. In `app/api/v1.py`, **append** exactly the single line `router.include_router(prompts.api.router, prefix="/prompts", tags=["prompts"])` to `build_v1_router()` (per rules.md §1 "Append-only edits to shared assembly files" and "The `/api/v1` router is incremental"). Do not modify any other line of `app/api/v1.py`. The effective route prefix is `/api/v1/prompts`.

Implementation requirements: `GET /prompts` lists `{id, versions[]}`; `GET /prompts/{id}/{version}` returns metadata only (no template body if a `confidential: true` flag is set in YAML; not used in Phase 2 but recognized). The module router is declared with no prefix; the `/prompts` prefix is applied at the mount site. Requires authenticated admin scope (`prompts:read`).

Tests required: 401 unauth; 200 with admin scope; not present in OpenAPI for anonymous.

Otherwise standard.

---

### Cross-links

- Global rules and execution discipline: [`../rules.md`](../rules.md)
- Reviewer model instructions and final review checklist: [`../review.md`](../review.md)
- Plan overview, dependency map, and task index: [`../../../IMPLEMENTATION_PLAN.md`](../../../IMPLEMENTATION_PLAN.md)
