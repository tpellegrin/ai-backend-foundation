# Task T-1602 — `app/rag/api.py` POST `/rag/ask`

> Section: **S16 — RAG**
>
> Before you start, read [`../rules.md`](../rules.md) (global rules, stop signals, allowed-files discipline, command discipline, lower-complexity model instructions).
> After you finish, self-check against [`../review.md`](../review.md) (final review checklist, architecture compliance).

---


Purpose: the second golden-path endpoint.

Depends on: T-1601, T-501.

Allowed files: `app/rag/api.py`, `app/api/v1.py` (append-only), tests. In `app/api/v1.py`, **append** exactly the single line `router.include_router(rag.api.router, prefix="/rag", tags=["rag"])` to `build_v1_router()` (per rules.md §1 "Append-only edits to shared assembly files" and "The `/api/v1` router is incremental"). Do not modify any other line of `app/api/v1.py`. The effective route path for the ask endpoint is `POST /api/v1/rag/ask`.

Implementation requirements: Pydantic request `{question: str, top_k: int = 5, filters: dict | None}`; response `{answer: str, citations: [{document_id, chunk_id, source_uri, page, section, preview}], request_id: str}`. `X-Budget-Warning` header propagated if present. The module router is declared with no prefix; the `/rag` prefix is applied at the mount site.

Tests required: see T-1603.

Otherwise standard.

---

### Cross-links

- Global rules and execution discipline: [`../rules.md`](../rules.md)
- Reviewer model instructions and final review checklist: [`../review.md`](../review.md)
- Plan overview, dependency map, and task index: [`../../../IMPLEMENTATION_PLAN.md`](../../../IMPLEMENTATION_PLAN.md)
