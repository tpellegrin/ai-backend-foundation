# Task T-1503 — `app/core/wiring/vector_store.py`

> Section: **S15 — Vector store**
>
> Before you start, read [`../rules.md`](../rules.md) (global rules, stop signals, allowed-files discipline, command discipline, lower-complexity model instructions).
> After you finish, self-check against [`../review.md`](../review.md) (final review checklist, architecture compliance).

---


Purpose: bind `VectorStore` port to pgvector adapter.

Depends on: T-1502.

Allowed files: `app/core/wiring/vector_store.py`, `app/core/container.py` (append-only), `app/core/lifespan.py` (append-only), `app/core/wiring/tests/test_wiring_vector_store.py`.

Forbidden: edits to `container.py` and `lifespan.py` are strictly append-only per rules.md §1 "Append-only edits to shared assembly files" — no reordering or rewriting of fields/hooks landed by earlier tasks.

Implementation requirements:

- The wiring function returns a Protocol-typed `VectorStore` (not the concrete pgvector adapter class).
- Append to `app/core/container.py`: a `vector_store: VectorStore` field.
- Append to `app/core/lifespan.py`: a vector-store readiness probe registered on the `ProbeRegistry` (per rules.md §1 incremental-probe rule). The pgvector adapter reuses the DB engine already managed by T-701's lifespan hook; do not open a second engine here.

Tests required: wiring smoke test confirms the container's `vector_store` field is populated and Protocol-typed; readiness probe for the vector store is present in the registry.

Otherwise standard.

---

### Cross-links

- Global rules and execution discipline: [`../rules.md`](../rules.md)
- Reviewer model instructions and final review checklist: [`../review.md`](../review.md)
- Plan overview, dependency map, and task index: [`../../../IMPLEMENTATION_PLAN.md`](../../../IMPLEMENTATION_PLAN.md)
