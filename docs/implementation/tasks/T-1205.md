# Task T-1205 — `app/ai_governance/api.py` (read-only)

> Section: **S12 — ai_governance persistence/service/api + OpenAI adapters**
>
> Before you start, read [`../rules.md`](../rules.md) (global rules, stop signals, allowed-files discipline, command discipline, lower-complexity model instructions).
> After you finish, self-check against [`../review.md`](../review.md) (final review checklist, architecture compliance).

---


Purpose: admin visibility.

Depends on: T-1203, T-906.

Allowed files: `app/ai_governance/api.py`, `app/api/v1.py` (append-only), tests. In `app/api/v1.py`, **append** exactly the single line `router.include_router(ai_governance.api.router, prefix="/governance", tags=["governance"])` to `build_v1_router()` (per rules.md §1 "Append-only edits to shared assembly files" and "The `/api/v1` router is incremental"). Do not modify any other line of `app/api/v1.py`. The effective route prefix is `/api/v1/governance`.

Implementation requirements: `GET /budgets` and `GET /usage` (per current tenant); admin-only scope (`governance:read`). The module router is declared with no prefix; the `/governance` prefix is applied at the mount site. Effective paths: `GET /api/v1/governance/budgets`, `GET /api/v1/governance/usage`.

Otherwise standard.

---

### Cross-links

- Global rules and execution discipline: [`../rules.md`](../rules.md)
- Reviewer model instructions and final review checklist: [`../review.md`](../review.md)
- Plan overview, dependency map, and task index: [`../../../IMPLEMENTATION_PLAN.md`](../../../IMPLEMENTATION_PLAN.md)
